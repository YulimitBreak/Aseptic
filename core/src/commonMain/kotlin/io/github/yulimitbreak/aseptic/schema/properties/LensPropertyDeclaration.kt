package io.github.yulimitbreak.aseptic.schema.properties

/**
 * Used to declare lens fields. As lenses don't exist at runtime, no data needs to be stored —
 * this object is used only as a stub for type detection, while code generation uses
 * arguments directly from the constructor method.
 *
 * @see [io.github.yulimitbreak.aseptic.schema.AsepticSchema.lens]
 */
object LensPropertyDeclaration
